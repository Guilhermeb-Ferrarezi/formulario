{
  "name": "santos-tech",
  "private": true,
  "scripts": {
    "build": "cd web && bun install && bun run build",
    "start": "cd api && bun install && bun run start",
    "deploy": "bun run build && bun run start"
  }
}